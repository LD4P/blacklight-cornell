<% if params["info_type"].present? && params[:info_type] == "dashboard"  %>
    <%= render 'author_info', :response => @headingsResponse %>
<% else %>
    <%= render 'other_info', :response => @headingsResponse %>
<% end %>
